FROM alpine:3.9
ARG VERSION
ARG COMPONENT

COPY /bin/rpc-* /bin/grpc

RUN echo ${VERSION}
RUN echo ${COMPONENT}
ENV GRPC_PORT :7080
ENV VIRTUAL_PROTO grpc
ENV VIRTUAL_HOST ${COMPONENT}-${VERSION}.grpc.apiatm.com
EXPOSE 7080
CMD ["grpc"]